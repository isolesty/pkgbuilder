#! /bin/bash

declare -r SVERSION="v0.5.0"

declare ARCH=$(dpkg-architecture -qDEB_HOST_ARCH)
declare DEBBUILDOPTS="-d -us -uc -sa -jauto"

declare -ra ARCHITECTURES=(
    mips64el
    # should be sw64
    sw_64
    arm64
)
declare -rA DISTRIBUTIONS=(
    ['mips64el']='unstable'
    ['sw_64']='unstable'
    ['arm64']='testing'
)

declare -r WORKBASE=${scriptdir%/*}
